server:
  host: "0.0.0.0"
  port: "8080"
  mode: "debug" # debug, release, test

database:
  host: "localhost"
  port: 3306
  user: "root"
  password: "password"
  dbname: "gym_admin"
  charset: "utf8mb4"

redis:
  host: "localhost"
  port: 6379
  password: ""
  db: 0

jwt:
  secret: "your-secret-key-change-in-production"
  expire_time: 86400 # 24 hours in seconds

log:
  level: "info"
  filename: "logs/app.log"
  max_size: 100 # MB
  max_backups: 10
  max_age: 30 # days

oss:
  endpoint: "oss-cn-hangzhou.aliyuncs.com"
  access_key_id: ""
  access_key_secret: ""
  bucket_name: "gym-admin"
